from utils.taxonomy import Taxonomy
from utils.item import Item
from typing import Set


def basic_ar(transactions: Set, taxonomy: Taxonomy, min_interest: float, min_sup: float, min_conf: float):
    pass
